// Package router deterministically classifies query intent and recommends an
// upstream retrieval mode and context budget. It uses a fixed rule order and
// returns reason strings for the first matched rule.
package router
